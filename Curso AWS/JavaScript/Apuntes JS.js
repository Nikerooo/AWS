/* Crear variable =  var nombreVariable;   Luego se indica de que tipo es la variable cargandole un dato  var nomVar = 5;

console.log(datoMostrar); Sirve para mostrar cosas en la consola

variable = prompt ("Texto a mostrar");  Sirve para que el usuario ingrese datos, como scanf

if (condition) {    
    // qué hacer si la condición es true
}
else if (2nd condition) { // puede tener 0 a muchas de estas sentencias    
    // qué hacer si la segunda condición es true
}
else {  // puede tener 0 o 1 de estas sentencias    
    // qué hacer si no se cumple ninguna de las condiciones anteriores
}

for(var i=0; i<10; i++){
    (Contenido)                   //Se maneja como en C
}

while (condition) {    
    // qué seguir haciendo mientras la condición sea verdadera
}                                                                                         //Mientras
// llegamos a esta línea cuando la condición en el ciclo es falsa


function nameOfFunction() {        
        // código a ejecutarse          Funciones
}

array.pop              saca el ultimo elemento del array
array.push(dato)       introduce un dato al ultimo elemento del array
array.lenght           devuelve el tamaño del array
*/